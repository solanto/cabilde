const MaterialIcon = ({ icon }) =>
    <span className="material-icons-round usa-icon"
        aria-hidden="true"
        focusable="false"
        role="img">
        {icon}
    </span>

export default MaterialIcon
